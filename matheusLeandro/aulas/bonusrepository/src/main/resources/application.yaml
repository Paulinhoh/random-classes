server:
  port: 8080

spring:
  application:
    name: bonusrepository

  datasource:
    url: jdbc:h2:mem:meubanco
    username: admin
    password: admin123
    driver-class-name: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true

  h2:
    console:
      enabled: true